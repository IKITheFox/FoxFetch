# 为 FoxFetch 做出贡献

[English](CONTRIBUTING.md) | 简体中文

## 开始前

反馈缺陷前请搜索已有 Issue。较大的新功能或架构调整，请先通过 Issue 确认范围。安全漏洞应通过[私密报告渠道](SECURITY.zh-CN.md)提交，不要公开细节。

提交内容必须由你创作，或具备合法提交权限。FoxFetch 项目贡献采用 GPL-3.0-only；第三方代码保留原许可与声明。本指南不另行要求签署贡献者许可协议。

## 开发流程

1. Fork 仓库，创建针对单一问题的分支。
2. 安装 `package.json` 声明的 Node.js 与 pnpm。
3. 运行 `pnpm install --frozen-lockfile`。
4. 使用 `pnpm dev`，或运行 `pnpm build` 后在 Chrome 中加载 `.output/chrome-mv3`。
5. 将修改限制在本次问题范围内。

提交前运行：

```sh
pnpm typecheck
pnpm test
pnpm build
```

使用仓库的 Prettier 与 ESLint 配置检查修改文件，不要格式化无关文件。修改依赖时同步提交锁文件。依赖补丁保存在 `patches/`，不要将直接修改 `node_modules` 作为修复交付。

## 界面与下载改动

- 同时修改英文与简体中文文案，不翻译来源提供的媒体标题。
- 检查窄屏布局及浅色、深色主题，截图不得包含私人浏览或账号信息。
- 为修改行为补充回归测试。
- 下载改动需记录实际分辨率、编码／封装组合、结果及本地播放检查。单元测试不能证明真实网站兼容性。
- 不引入 DRM 绕过、认证绕过、远程可执行代码或非必要权限。
- 不提交凭据、Cookie、带签名的媒体链接、浏览器资料、生成安装包或私人诊断日志。

## Pull Request

说明问题、改动、测试方法及剩余限制，关联对应 Issue。填写检查清单，并明确哪些检查未运行，不得把未执行的测试记为通过。

合并前由维护者审核。文档或维护更新不会移动 Beta 发布标签。
